apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: argo.locals # Домен, по которому будете заходить
      http:
        paths:
          - path: /argo
            pathType: Prefix
            backend:
              service:
                name: argocd-repo-server
                port:
                  number: 8080
